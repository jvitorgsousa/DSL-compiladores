// Declaração e inicialização de variáveis
string cliente = "Joao";
float limite = 150.0;
int quantidade = 2;

// Adicionando itens ao carrinho (Nome do produto, Valor)
item "Camiseta", 2.0;
item "Calça", 1.0;

// Condicional simples
if (limite > 100.0) {
    resumo;
}

// Loop simples usando reatribuição direta
int i = 0;
while (i < 3) {
    i = 3; 
}